#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include "../header/tfs.h"
#include "../header/ll.h"

error init_partition(char* disk_name, int partition, uint32_t file_count);

error init_partition(char* disk_name, int partition, uint32_t file_count){
	return _NOERROR;
}